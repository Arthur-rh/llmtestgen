# Rename to .env and fill in the credentials before running llmtestgen.
# Or use llmtestgen-edit to open a simple editor.

#############################################################
#                  LLM MODELS PARAMETERS
#############################################################

# Your API key for OpenRouter; required to call OpenRouter-hosted models.
OPENROUTER_API_KEY=<replace-with-openrouter-key>

# Default model used when calling OpenRouter if no model is specified at runtime.
OPENROUTER_DEFAULT_MODEL=tngtech/deepseek-r1t2-chimera:free

# Your API key for OpenAI; required only if you want to use OpenAI models.
OPENAI_API_KEY=<replace-with-openai-key>

# Default OpenAI model used when none is explicitly chosen.
OPENAI_DEFAULT_MODEL=gpt-4o-mini

#############################################################
#                           MISC
#############################################################

# Environment mode for the application (development or production).
# Affects logging and debug behavior.
APP_ENV=development

# URL used by OpenRouter to identify your app.
# Do not change unless deploying behind a different public URL.
OPENROUTER_SITE_URL=http://localhost:8000

# Title sent to OpenRouter for analytics and attribution.
OPENROUTER_APP_TITLE=LLMTestGen

# OpenAI organization identifier (optional).
# Only required if your OpenAI account uses organizations.
OPENAI_ORG=LLMTestGen